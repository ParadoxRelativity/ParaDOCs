{
  "name": "@paradocs/mobile",
  "version": "0.13.3",
  "private": true,
  "description": "ParaDOCs mobile app for iOS and Android",
  "scripts": {
    "build:web": "npm run build --workspace=@paradocs/web",
    "sync": "npm run build:web && cap sync",
    "ios": "npm run sync && cap open ios",
    "android": "npm run sync && cap open android",
    "run:ios": "npm run sync && cap run ios",
    "run:android": "npm run sync && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "typescript": "^5.6.3"
  },
  "author": "ParaDOCs contributors",
  "license": "AGPL-3.0-or-later"
}
